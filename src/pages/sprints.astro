---
import BaseLayout from '../layouts/BaseLayout.astro';

const whatHappens = [
    "a clear build challenge (you can finish it)",
    "people around you building too",
    "practical help when you get stuck",
    "a demo at the end (yes, even if it's rough)"
];
---

<BaseLayout title="Sprints">
    <!-- Page Header -->
    <section class="page-header">
        <div class="container">
            <h1>SPRINTS</h1>
            <div class="page-intro">
                <p>Sprints are where talking stops.<br>You show up. You build. You ship.</p>
                <br>
                <p>They're short, intense build sessions — sometimes with partners — where we compress time and force momentum.</p>
            </div>
        </div>
    </section>

    <!-- What Happens -->
    <section class="what-happens-section">
        <div class="container">
            <h2>WHAT HAPPENS IN A SPRINT</h2>
            <ul class="what-list">
                {whatHappens.map(item => (
                    <li>{item}</li>
                ))}
            </ul>
        </div>
    </section>

    <!-- Bring / Leave -->
    <section class="bring-leave-section">
        <div class="container">
            <div class="bring-leave-grid">
                <div class="bring-leave-card card">
                    <h3>BRING</h3>
                    <p>A laptop, a problem worth solving, and a willingness to move fast.</p>
                </div>
                <div class="bring-leave-card card">
                    <h3>LEAVE WITH</h3>
                    <p>A shipped prototype, new contacts, and the next 3 actions.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Who It's For -->
    <section class="who-section">
        <div class="container">
            <div class="who-content">
                <p>If you're stuck, overthinking, or moving too slowly — do a sprint.<br>You'll surprise yourself.</p>
            </div>
        </div>
    </section>

    <!-- Page CTA -->
    <section class="page-cta-section">
        <div class="container">
            <div class="page-cta-block">
                <p>See upcoming sprints on the Events page.<br>And get the weekly email so you don't miss the next one.</p>
                <div class="cta-buttons">
                    <a href="/events" class="btn">View events</a>
                    <a href="#newsletter" class="btn btn--outline">Get the weekly email</a>
                </div>
            </div>
        </div>
    </section>
</BaseLayout>

<style>
    /* PAGE HEADER */
    .page-header {
        padding-top: 140px;
        padding-bottom: 3rem;
        border-bottom: 1px solid var(--border-color);
    }

    .page-header .container {
        max-width: 1000px;
    }

    .page-header h1 {
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        margin-bottom: 1.5rem;
    }

    .page-intro {
        font-size: 1.1rem;
        max-width: 600px;
        color: #ccc;
        border-left: 2px solid var(--accent-color);
        padding-left: 1.5rem;
    }

    .page-intro p {
        max-width: none;
    }

    /* WHAT HAPPENS SECTION */
    .what-happens-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .what-happens-section .container {
        max-width: 1000px;
    }

    .what-happens-section h2 {
        font-size: 1.25rem;
        margin-bottom: 1.5rem;
    }

    .what-list {
        list-style: none;
        max-width: 500px;
    }

    .what-list li {
        font-size: 1.05rem;
        color: #ccc;
        margin-bottom: 0.75rem;
        padding-left: 1.5rem;
        position: relative;
    }

    .what-list li::before {
        content: "•";
        position: absolute;
        left: 0;
        color: var(--accent-color);
    }

    /* BRING / LEAVE SECTION */
    .bring-leave-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .bring-leave-section .container {
        max-width: 1000px;
    }

    .bring-leave-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
    }

    .bring-leave-card {
        padding: 2rem;
    }

    .bring-leave-card h3 {
        font-size: 1rem;
        font-family: var(--font-mono);
        color: var(--accent-color);
        margin-bottom: 1rem;
    }

    .bring-leave-card p {
        font-size: 1.05rem;
        color: #ccc;
        line-height: 1.6;
    }

    /* WHO SECTION */
    .who-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .who-section .container {
        max-width: 1000px;
    }

    .who-content p {
        font-size: 1.2rem;
        color: #ccc;
        line-height: 1.6;
        max-width: 600px;
    }

    /* PAGE CTA SECTION */
    .page-cta-section {
        padding: 4rem 0;
    }

    .page-cta-section .container {
        max-width: 1000px;
    }

    .page-cta-block {
        text-align: center;
    }

    .page-cta-block > p {
        font-size: 1.1rem;
        color: #aaa;
        margin-bottom: 2rem;
    }

    .cta-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn--outline {
        background: transparent;
        color: var(--text-color);
    }

    .btn--outline:hover {
        background: var(--text-color);
        color: var(--bg-color);
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
        .page-header {
            padding-top: 120px;
        }

        .cta-buttons {
            flex-direction: column;
        }

        .cta-buttons .btn {
            width: 100%;
        }
    }
</style>
