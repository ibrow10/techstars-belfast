---
import BaseLayout from '../layouts/BaseLayout.astro';

const latestEvent = {
    day: '03',
    month: 'FEB',
    tag: 'PARTNER SESSION',
    tagType: 'partner',
    title: 'Founder Labs x The AMP: AMA',
    time: '11:00 AM – 3:00 PM',
    location: 'Ebrington Square, Derry',
    description: 'Join the Founder Labs team at The AMP for an open conversation about Cohort 4.',
    link: 'https://luma.com/ixnvtb9s'
};

const whyCards = [
    {
        icon: 'users',
        title: 'Community',
        description: 'Rooms full of founders. Honest conversations. No posturing.',
        bg: '/images/founders1.jpg'
    },
    {
        icon: 'zap',
        title: 'Momentum',
        description: 'Sprints, AMAs, workshops — things that make you move faster.',
        bg: '/images/founders2.jpg'
    },
    {
        icon: 'globe',
        title: 'Reach',
        description: 'Warm pathways to mentors, investors and partners across the UK, Europe and North America.',
        bg: '/images/founders3.jpg'
    }
];

const startHereCards = [
    {
        title: 'Come to an event',
        description: 'Meet founders. Learn something useful. Leave with energy.',
        cta: 'View events',
        link: '/events'
    },
    {
        title: 'Join a sprint',
        description: 'Time-boxed build sessions. Ship something real.',
        cta: 'See sprints',
        link: '/sprints'
    },
    {
        title: 'Do a programme',
        description: 'Structured weeks that take you from "maybe" to "we\'re actually doing this".',
        cta: 'Explore programmes',
        link: '/programmes'
    }
];

const testimonials = [
    {
        quote: "This is phenomenal for Belfast, and NI overall. Well played! 🚀",
        author: "Founder, Northern Ireland"
    },
    {
        quote: "I left with a shipped prototype and three intros that actually mattered.",
        author: "Founder, Northern Ireland"
    }
];
---

<BaseLayout title="Home">
    <!-- Hero Section with Video Background -->
    <section class="hero">
        <div class="hero-video-container">
            <video
                class="hero-video"
                autoplay
                muted
                loop
                playsinline
            >
                <source src="/images/hero-techstars.mp4" type="video/mp4">
            </video>
            <div class="hero-overlay"></div>
        </div>

        <div class="container hero-content">
            <div class="hero-logo">
                <img src="/images/techstarsbelfastlogo.png" alt="Techstars Belfast">
            </div>
            
            <h1>BUILD FROM HERE.<br>SHIP TO EVERYWHERE.</h1>
            
            <p class="hero-tagline">
                The founder community Northern Ireland needs. Built here<span class="cursor">|</span>
            </p>

            <div class="hero-ctas">
                <a href="/events" class="btn">See what's on</a>
                <a href="#newsletter" class="btn btn--outline">Get the weekly email</a>
            </div>
        </div>
    </section>

    <!-- Why Founders Show Up -->
    <section class="why-section">
        <div class="container">
            <h2>Why founders show up</h2>
            
            <div class="why-grid">
                {whyCards.map(card => (
                    <div class="why-card" style={`--bg-image: url('${card.bg}')`}>
                        <div class="why-card-bg"></div>
                        <div class="why-card-content">
                            <div class="why-icon">
                                <i data-lucide={card.icon}></i>
                            </div>
                            <h3>{card.title}</h3>
                            <p>{card.description}</p>
                        </div>
                    </div>
                ))}
            </div>
        </div>
    </section>

    <!-- Start Here Section -->
    <section class="start-here-section">
        <div class="container">
            <div class="section-header">
                <h2>NOT SURE WHERE YOU FIT? START HERE.</h2>
            </div>

            <div class="start-here-grid">
                {startHereCards.map(card => (
                    <div class="start-here-card card">
                        <h3>{card.title}</h3>
                        <p>{card.description}</p>
                        <a href={card.link} class="card-link">{card.cta} →</a>
                    </div>
                ))}
            </div>
        </div>
    </section>

    <!-- Ethos Strip -->
    <section class="ethos-strip">
        <div class="container">
            <p>We give first. We move fast. We're here to help founders win.</p>
        </div>
    </section>

    <!-- Manifesto Block -->
    <section class="manifesto-section">
        <div class="container">
            <div class="manifesto-box">
                <span class="manifesto-label">OUR DNA</span>
                <h2 class="manifesto-headline">Techstars Belfast is <span class="highlight">founder-led</span>.</h2>
                <p class="manifesto-subhead">We're a startup community that behaves like a startup.</p>
                
                <div class="manifesto-values">
                    <div class="value-block">
                        <p>We move fast. We're comfortable in ambiguity. We occasionally get things wrong.</p>
                        <p class="value-emphasis">When we do, we don't blame anyone — we fix it, learn, and move on.</p>
                    </div>
                    
                    <div class="value-block">
                        <p>Founders are our customer. We ask <span class="highlight">"how can we better help founders?"</span> every day.</p>
                        <p>And we don't care about borders — we're the bridge from Belfast into the UK, Europe and North America.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Next Up (Featured Event) -->
    <section id="events" class="events-section">
        <div class="container">
            <div class="section-header">
                <h2>NEXT UP</h2>
            </div>

            <p class="section-intro">Turn up. Bring questions. Leave clearer.</p>

            <div class="events-grid">
                <div class="event-card">
                    <div class="event-date-box">
                        <span class="day">{latestEvent.day}</span>
                        <span class="month">{latestEvent.month}</span>
                    </div>
                    <div class="event-content">
                        <div class="tag-row">
                            <span class={`tag-pill ${latestEvent.tagType}`}>{latestEvent.tag}</span>
                        </div>
                        <h3>{latestEvent.title}</h3>
                        <div class="event-info-row">
                            <div class="info-item">
                                <i data-lucide="clock"></i>
                                <span>{latestEvent.time}</span>
                            </div>
                            <div class="info-item">
                                <i data-lucide="map-pin"></i>
                                <span>{latestEvent.location}</span>
                            </div>
                        </div>
                        <p class="event-description">{latestEvent.description}</p>
                    </div>
                    <div class="event-action">
                        <a href={latestEvent.link} class="btn" target="_blank">REGISTER NOW</a>
                    </div>
                </div>
            </div>

            <div class="view-all-wrapper">
                <a href="/events" class="btn">VIEW ALL EVENTS →</a>
            </div>
        </div>
    </section>

    <!-- Newsletter Section -->
    <section id="newsletter" class="newsletter-section">
        <div class="container">
            <div class="newsletter-block">
                <h2>THE WEEKLY FOUNDER EMAIL</h2>
                <p>One email a week. No spam. Just the best founder opportunities in NI.</p>
                <p class="newsletter-detail">Events. Sprints. Programmes. Open calls. Things worth your time.</p>
                <a href="#newsletter" class="btn btn--accent">Get the weekly email</a>
            </div>
        </div>
    </section>

    <!-- Testimonials Section -->
    <section class="testimonials-section">
        <div class="container">
            <div class="section-header">
                <h2>WHAT FOUNDERS SAY</h2>
            </div>

            <div class="testimonials-grid">
                {testimonials.map(testimonial => (
                    <div class="testimonial-card card">
                        <p class="testimonial-quote">"{testimonial.quote}"</p>
                        <p class="testimonial-author">— {testimonial.author}</p>
                    </div>
                ))}
            </div>
        </div>
    </section>

</BaseLayout>

<style>
    /* BLINKING CURSOR */
    .cursor {
        color: var(--accent-color);
        font-weight: 400;
        animation: blink 1s step-end infinite;
    }

    @keyframes blink {
        0%, 100% { opacity: 1; }
        50% { opacity: 0; }
    }

    /* HERO SECTION */
    .hero {
        position: relative;
        min-height: 50vh;
        display: flex;
        align-items: flex-start;
        overflow: hidden;
        padding-top: 100px;
        padding-bottom: 4rem;
    }

    .hero .container {
        max-width: 1000px;
        margin: 0 auto;
        padding: 0 2rem;
    }

    .hero-video-container {
        position: absolute;
        inset: 0;
        z-index: 0;
    }

    .hero-video {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .hero-overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(
            to bottom,
            rgba(15, 15, 15, 0.75) 0%,
            rgba(15, 15, 15, 0.85) 60%,
            rgba(15, 15, 15, 1) 100%
        );
    }

    .hero-content {
        position: relative;
        z-index: 1;
        padding-top: 100px;
        width: 100%;
    }

    .hero-logo {
        margin-bottom: 2.5rem;
        padding-top: 1rem;
    }

    .hero-logo img {
        height: 100px;
        width: auto;
    }

    .hero h1 {
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        line-height: 1.1;
        margin-bottom: 1.5rem;
        max-width: 800px;
    }

    .hero-tagline {
        font-size: 1.3rem;
        max-width: 600px;
        color: #ccc;
        margin-bottom: 2rem;
        line-height: 1.5;
    }

    .hero-ctas {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .btn--outline {
        background: transparent;
        color: var(--text-color);
    }

    .btn--outline:hover {
        background: var(--text-color);
        color: var(--bg-color);
    }

    /* WHY FOUNDERS SHOW UP SECTION */
    .why-section {
        padding: 5rem 0;
        border-bottom: 1px solid var(--border-color);
        background: linear-gradient(to bottom, #0f0f0f 0%, #141414 100%);
    }

    .why-section .container {
        max-width: 1000px;
    }

    .why-section h2 {
        text-align: center;
        margin-bottom: 3.5rem;
        font-size: 1.25rem;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: var(--text-muted);
    }

    .why-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 2.5rem;
    }

    .why-card {
        position: relative;
        text-align: center;
        padding: 2.5rem 1.5rem;
        border-radius: 16px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        transition: all 0.3s ease;
        overflow: hidden;
        min-height: 320px;
        display: flex;
        align-items: stretch;
    }

    .why-card-bg {
        position: absolute;
        inset: 0;
        background-image: var(--bg-image);
        background-size: cover;
        background-position: center;
        opacity: 0.25;
        transition: opacity 0.3s ease;
    }

    .why-card-bg::after {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(to top, rgba(15, 15, 15, 0.95) 0%, rgba(15, 15, 15, 0.7) 50%, rgba(15, 15, 15, 0.5) 100%);
    }

    .why-card-content {
        position: relative;
        z-index: 1;
        width: 100%;
        display: flex;
        flex-direction: column;
        height: 100%;
    }

    .why-card .why-icon {
        margin-top: auto;
    }

    .why-card p {
        margin-bottom: 0;
    }

    .why-card:hover {
        border-color: rgba(0, 210, 106, 0.4);
        transform: translateY(-4px);
    }

    .why-card:hover .why-card-bg {
        opacity: 0.35;
    }

    .why-icon {
        width: 56px;
        height: 56px;
        margin: 0 auto 1.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 12px;
        background: rgba(0, 230, 118, 0.1);
        color: var(--accent-color);
    }

    .why-icon i {
        width: 28px;
        height: 28px;
    }

    .why-card h3 {
        color: #fff;
        font-size: 1.25rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: 1rem;
        font-family: var(--font-mono);
    }

    .why-card p {
        color: #999;
        line-height: 1.7;
        font-size: 1rem;
    }

    /* START HERE SECTION */
    .start-here-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .start-here-section .container {
        max-width: 1000px;
    }

    .start-here-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
    }

    .start-here-card {
        padding: 2rem;
    }

    .start-here-card h3 {
        font-size: 1.2rem;
        margin-bottom: 0.75rem;
        font-family: var(--font-mono);
    }

    .start-here-card p {
        font-size: 0.95rem;
        color: #aaa;
        margin-bottom: 1.5rem;
        line-height: 1.5;
    }

    .card-link {
        font-family: var(--font-mono);
        font-size: 0.85rem;
        color: var(--accent-color);
        text-transform: uppercase;
        transition: opacity 0.2s ease;
    }

    .card-link:hover {
        opacity: 0.8;
    }

    /* ETHOS STRIP */
    .ethos-strip {
        padding: 3rem 0;
        background: #161616;
        border-bottom: 1px solid var(--border-color);
    }

    .ethos-strip .container {
        max-width: 1000px;
    }

    .ethos-strip p {
        font-family: var(--font-mono);
        font-size: 1.2rem;
        text-align: center;
        color: var(--accent-color);
        margin: 0;
    }

    /* MANIFESTO SECTION */
    .manifesto-section {
        padding: 5rem 0;
        border-bottom: 1px solid var(--border-color);
        background: linear-gradient(135deg, rgba(0, 210, 106, 0.03) 0%, transparent 50%);
    }

    .manifesto-section .container {
        max-width: 1000px;
    }

    .manifesto-box {
        background: #111;
        border: 1px solid var(--border-color);
        border-left: 4px solid var(--accent-color);
        border-radius: 0 12px 12px 0;
        padding: 3rem;
        position: relative;
    }

    .manifesto-label {
        display: inline-block;
        font-family: var(--font-mono);
        font-size: 0.7rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        color: var(--accent-color);
        background: rgba(0, 210, 106, 0.1);
        padding: 6px 12px;
        border-radius: 4px;
        margin-bottom: 1.5rem;
    }

    .manifesto-headline {
        font-size: clamp(1.8rem, 4vw, 2.5rem);
        color: #fff;
        margin-bottom: 0.5rem;
        line-height: 1.2;
    }

    .manifesto-subhead {
        font-size: 1.3rem;
        color: #888;
        margin-bottom: 2.5rem;
        padding-bottom: 2rem;
        border-bottom: 1px solid var(--border-color);
    }

    .manifesto-values {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2.5rem;
    }

    .value-block p {
        font-size: 1.1rem;
        color: #999;
        line-height: 1.7;
        margin-bottom: 1rem;
    }

    .value-emphasis {
        color: #ccc !important;
        font-weight: 500;
    }

    .highlight {
        color: var(--accent-color);
    }

    /* EVENTS SECTION */
    .events-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .events-section .container {
        max-width: 1000px;
    }

    .section-header {
        margin-bottom: 1.5rem;
        border-bottom: 1px solid var(--border-color);
        padding-bottom: 1rem;
    }

    .section-intro {
        font-size: 1.1rem;
        color: #aaa;
        margin-bottom: 2rem;
    }

    .events-grid {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .event-card {
        display: grid;
        grid-template-columns: 120px 1fr auto;
        gap: 2rem;
        background: #161616;
        border: 1px solid var(--border-color);
        border-radius: 12px;
        padding: 2rem;
        transition: all 0.3s ease;
        align-items: center;
    }

    .event-card:hover {
        border-color: var(--accent-color);
        transform: translateY(-2px);
        box-shadow: 0 8px 30px rgba(0,0,0,0.4);
        background: #1c1c1c;
    }

    .event-date-box {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        background: #222;
        border-radius: 8px;
        padding: 1rem;
        min-height: 100px;
        border: 1px solid #333;
    }

    .event-date-box .day {
        font-size: 2rem;
        font-weight: 800;
        color: var(--accent-color);
        line-height: 1;
        font-family: var(--font-mono);
    }

    .event-date-box .month {
        font-size: 0.75rem;
        color: #888;
        margin-top: 0.25rem;
        letter-spacing: 0.1em;
        font-family: var(--font-mono);
        text-transform: uppercase;
    }

    .event-content {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .event-content h3 {
        font-size: 1.5rem;
        color: #fff;
        margin: 0;
        letter-spacing: -0.01em;
        font-family: var(--font-main);
        text-transform: none;
    }

    .event-info-row {
        display: flex;
        flex-wrap: wrap;
        gap: 1.5rem;
        margin-top: 0.5rem;
    }

    .info-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.85rem;
        color: #aaa;
        font-family: var(--font-mono);
    }

    .info-item i {
        width: 16px;
        height: 16px;
        color: var(--accent-color);
    }

    .event-description {
        font-size: 0.95rem;
        color: #888;
        line-height: 1.5;
        margin-top: 0.5rem;
    }

    .tag-pill {
        display: inline-flex;
        align-items: center;
        padding: 2px 10px;
        border-radius: 20px;
        font-size: 0.7rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        font-family: var(--font-mono);
    }

    .tag-pill.remote { background: rgba(108, 92, 231, 0.2); color: #a29bfe; }
    .tag-pill.in-person { background: rgba(0, 210, 106, 0.1); color: #00D26A; }
    .tag-pill.partner { background: rgba(255, 255, 255, 0.1); color: #fff; border: 1px solid rgba(255,255,255,0.3); }

    .event-action .btn {
        min-width: 120px;
        text-align: center;
        border-radius: 6px;
        font-size: 0.8rem;
        padding: 12px 20px;
    }

    .view-all-wrapper {
        margin-top: 3rem;
        text-align: center;
    }

    /* NEWSLETTER SECTION */
    .newsletter-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .newsletter-section .container {
        max-width: 1000px;
    }

    .newsletter-block {
        background: #0a0a0a;
        border: 1px solid var(--border-color);
        border-radius: 12px;
        padding: 4rem;
        text-align: center;
    }

    .newsletter-block h2 {
        color: var(--accent-color);
        margin-bottom: 1rem;
    }

    .newsletter-block p {
        font-size: 1.15rem;
        color: #ccc;
        margin-bottom: 0.5rem;
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
    }

    .newsletter-detail {
        font-size: 1rem !important;
        color: #888 !important;
        margin-bottom: 2rem !important;
    }

    .btn--accent {
        background: var(--accent-color);
        color: var(--bg-color);
        border-color: var(--accent-color);
    }

    .btn--accent:hover {
        background: transparent;
        color: var(--accent-color);
    }

    /* TESTIMONIALS SECTION */
    .testimonials-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .testimonials-section .container {
        max-width: 1000px;
    }

    .testimonials-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
    }

    .testimonial-card {
        padding: 2rem;
    }

    .testimonial-quote {
        font-size: 1.1rem;
        color: #ccc;
        line-height: 1.6;
        margin-bottom: 1rem;
        font-style: italic;
    }

    .testimonial-author {
        font-family: var(--font-mono);
        font-size: 0.85rem;
        color: var(--accent-color);
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
        .hero-content {
            padding-top: 120px;
        }

        .hero-ctas {
            flex-direction: column;
        }

        .hero-ctas .btn {
            width: 100%;
            text-align: center;
        }

        .why-grid {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        .why-card {
            padding: 2rem 1.5rem;
        }

        .event-card {
            grid-template-columns: 1fr;
            gap: 1.5rem;
            padding: 1.5rem;
        }

        .event-date-box {
            flex-direction: row;
            min-height: auto;
            padding: 0.5rem 1rem;
            gap: 1rem;
            justify-content: flex-start;
        }

        .event-date-box .month {
            margin-top: 0;
        }

        .event-info-row {
            gap: 1rem;
        }

        .event-action .btn {
            width: 100%;
        }

        .newsletter-block {
            padding: 2rem;
        }
    }
</style>
