---
import BaseLayout from '../layouts/BaseLayout.astro';

const partners = [
    {
        name: "Queen's University Belfast (QUB)",
        description: "Talent, research, facilities — and a serious commitment to building the pipeline."
    },
    {
        name: "Ormeau Labs",
        description: "The home base. The rooms. The people."
    },
    {
        name: "Techstars",
        description: "Global network. Proven community systems. Give First."
    }
];
---

<BaseLayout title="About">
    <!-- Page Header -->
    <section class="page-header">
        <div class="container">
            <h1>ABOUT</h1>
            <div class="page-intro">
                <p>Techstars Belfast is founder-led.<br>We're building the community we wish existed when we started.</p>
                <br>
                <p>We give first with no expectation of return.<br>We care about founders more than we care about optics.</p>
            </div>
        </div>
    </section>

    <!-- What We're Building -->
    <section class="about-section">
        <div class="container">
            <h2>WHAT WE'RE BUILDING</h2>
            <div class="section-content">
                <p>Belfast is ready for a new chapter. So we're bringing Techstars' community playbook here — with Queen's University Belfast and Ormeau Labs — and adapting it for local founders.</p>
                <p>This isn't about "community strategy". It's about helping founders start companies that can compete globally.</p>
            </div>
        </div>
    </section>

    <!-- How We Work -->
    <section class="about-section">
        <div class="container">
            <h2>HOW WE WORK</h2>
            <div class="section-content">
                <p>We move fast. We're comfortable with ambiguity. We occasionally get things wrong. When we do, we fix it, learn, and keep going.</p>
                <p class="highlight">We ask ourselves, every day:<br>"How can we better help founders?"</p>
            </div>
        </div>
    </section>

    <!-- Partners -->
    <section class="partners-section">
        <div class="container">
            <h2>OUR PARTNERS</h2>
            <div class="partners-grid">
                {partners.map(partner => (
                    <div class="partner-item card">
                        <h3>{partner.name}</h3>
                        <p>{partner.description}</p>
                    </div>
                ))}
            </div>
            <div class="partner-logos">
                <img src="/images/QUB-Logo.png" alt="Queen's University Belfast" class="partner-logo">
                <img src="/images/ormeau-labs-logo.svg" alt="Ormeau Labs" class="partner-logo">
            </div>
        </div>
    </section>

    <!-- Closing CTA -->
    <section class="cta-section">
        <div class="container">
            <div class="cta-block">
                <h2>WANT IN?</h2>
                <p>Turn up to an event. Join a sprint. Or reach out if you want to build with us.</p>
                <div class="cta-buttons">
                    <a href="/events" class="btn">View events</a>
                    <a href="/sprints" class="btn btn--outline">See sprints</a>
                    <a href="/partners" class="btn btn--outline">Partner with us</a>
                </div>
            </div>
        </div>
    </section>
</BaseLayout>

<style>
    /* PAGE HEADER */
    .page-header {
        padding-top: 140px;
        padding-bottom: 3rem;
        border-bottom: 1px solid var(--border-color);
    }

    .page-header .container {
        max-width: 1000px;
    }

    .page-header h1 {
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        margin-bottom: 1.5rem;
    }

    .page-intro {
        font-size: 1.1rem;
        max-width: 600px;
        color: #ccc;
        border-left: 2px solid var(--accent-color);
        padding-left: 1.5rem;
    }

    .page-intro p {
        max-width: none;
    }

    /* ABOUT SECTIONS */
    .about-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .about-section .container {
        max-width: 1000px;
    }

    .about-section h2 {
        font-size: 1.25rem;
        margin-bottom: 1.5rem;
    }

    .section-content p {
        font-size: 1.1rem;
        color: #ccc;
        line-height: 1.7;
        margin-bottom: 1rem;
        max-width: 700px;
    }

    .section-content .highlight {
        font-size: 1.15rem;
        color: #fff;
        margin-top: 1.5rem;
    }

    /* PARTNERS SECTION */
    .partners-section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
    }

    .partners-section .container {
        max-width: 1000px;
    }

    .partners-section h2 {
        font-size: 1.25rem;
        margin-bottom: 2rem;
    }

    .partners-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
        margin-bottom: 3rem;
    }

    .partner-item {
        padding: 2rem;
    }

    .partner-item h3 {
        color: #fff;
        font-size: 1.1rem;
        margin-bottom: 0.75rem;
        font-family: var(--font-mono);
    }

    .partner-item p {
        font-size: 0.95rem;
        color: #aaa;
        line-height: 1.5;
    }

    .partner-logos {
        display: flex;
        align-items: center;
        gap: 2.5rem;
    }

    .partner-logo {
        height: 50px;
        width: auto;
        filter: brightness(0) invert(1);
        opacity: 0.7;
    }

    /* CTA SECTION */
    .cta-section {
        padding: 4rem 0;
    }

    .cta-section .container {
        max-width: 1000px;
    }

    .cta-block {
        text-align: center;
    }

    .cta-block h2 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
    }

    .cta-block > p {
        font-size: 1.1rem;
        color: #aaa;
        margin-bottom: 2rem;
    }

    .cta-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn--outline {
        background: transparent;
        color: var(--text-color);
    }

    .btn--outline:hover {
        background: var(--text-color);
        color: var(--bg-color);
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
        .page-header {
            padding-top: 120px;
        }

        .cta-buttons {
            flex-direction: column;
        }

        .cta-buttons .btn {
            width: 100%;
        }

        .partner-logos {
            flex-direction: column;
            align-items: flex-start;
            gap: 1.5rem;
        }
    }
</style>
